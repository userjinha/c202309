#include <stdio.h>

int main(void) {
	char c = 'A';
	// 문자형 변수 출력, 문자형 변수 유형인 char로 선언된 c 변수를 초기화
	printf("%c\n", c);
	// printf 함수를 사용하여 문자형 출력, %c는 문자 값을 출력하기 위한 서식 지정자
	// , 다음에 'c'변수를 전달하여 c변수의 값을 출력
	return 0;
}
