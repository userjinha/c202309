#include <stdio.h>
// 표준 입력 및 출력 함수를 사용할 수 있도록 
int main(void) {
	int age = 20;
	// 정수형 변수 선언 및 초기화
	printf("%d\n", age);
	// 정수형 변수의 값 출력하기, %d : 정수 값을 출력하기 위한 서식 지정, ','는 다음에
	// age 변수를 전달하여 age 변수의 값을 출력합니다, \n은 줄 바꿈 문자
	return 0;
}
